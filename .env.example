# Required: OpenAI API Key (get from platform.openai.com)
OPENAI_API_KEY=sk-proj-...

# Optional: Vercel AI Gateway Key (get from vercel.com dashboard → AI → Gateway)
# When set, all AI calls route through the gateway for unified monitoring
AI_GATEWAY_API_KEY=
